---
created: 2026-05-01T16:40:53.092Z
session: 3ce7bd17-41d4-486f-81d6-0cdb5f04f529
working_directory: C:\Users\knguy139\Documents\Projects
---

## Refactored File: `OPR_Auth_import_format_refactored.sql`

### v10 vs v11: No differences
They are identical SQL — same base CTE, same dedup qualify, same first_rows logic, same aggregated CTE, same final SELECT and ORDER BY. The only difference is the table name (`_v10` vs `_v11`). v11 appears to be a clean re-paste of v10 after confirming the logic worked.

### Refactored file structure (~150 lines, down from ~1,766)

**Step 1 — Format raw import** (lines 67–98 of original)
- Source: `tmp_1m.opr_mr_auth_202409_20251031_v2`
- Output: `tmp_1m.opr_mr_auth_202409_20251031_v2_formatted`
- `select distinct` with date parsing, TIN padding, patient_id cleanup, visit categorization, `auto_approval` flag
- Validation query: row count + distinct patient count

**Step 2 — Dedup + aggregate to episode level** (check_v11)
- Source: `_v2_formatted`
- Output: `tmp_1m.kn_opr_auth_request_check_v11`
- `base` CTE: episode key = `patient_id_prov_tin_prov_specialty`, dedup on `(patient_id, prov_tin, prov_specialty, date_received_dt, req_start_dt, req_end_dt)` via `qualify row_number() = 1`
- `first_rows` CTE: earliest row per episode for diagcode, review_decision, auto_approval_flag
- `aggregated` CTE: min/max dates, `count(*)`, `count(distinct auth_id)`, `sum(visit_req/visit_auth)`, plus first-row fields via left join
- Final select: episode_duration, first/last month columns
- Validation query: row count, distinct episodes, sum of total_requests

### What gets dropped
- v1–v10 check tables (all superseded)
- All ad-hoc SELECT queries (~40+ exploratory queries)
- All sample/report subset tables (`_sample`, `_rpt`, `_dedup`, `_sample_send`)
- The v1 format step (superseded by v2 format)
- TRE join steps (per your instruction)
- The `visit_req` → `next_visit_req` transition analysis query

### Formatting cleanup
- `<>` → `!=`
- Consistent leading-comma indentation
- Lowercase flag column names in comments